apiVersion: v1
kind: Service
metadata:
  name: {{ include "aula-ecr-helm.fullname" . }}
  labels:
    {{- include "aula-ecr-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aula-ecr-helm.selectorLabels" . | nindent 4 }}
