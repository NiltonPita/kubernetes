apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aula-ecr-helm.fullname" . }}-test-connection"
  labels:
    {{- include "aula-ecr-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aula-ecr-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
